name: Merge Develop to Main

on:
  workflow_dispatch:
    inputs:
      trigger:
        description: 'Триггер для запуска'
        required: true
        default: 'manual'
        type: string

jobs:
  merge:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      with:
        fetch-depth: 0
        token: ${{ secrets.GITHUB_TOKEN }}
    
    - name: Configure Git
      run: |
        git config --global user.email "github-actions[bot]@users.noreply.github.com"
        git config --global user.name "github-actions[bot]"
    
    - name: Get commits to merge
      id: get-commits
      run: |
        git fetch origin develop main
        COMMITS=$(git log --pretty=format:"- %s" origin/main..origin/develop)
        echo "COMMITS<<EOF" >> $GITHUB_OUTPUT
        echo "$COMMITS" >> $GITHUB_OUTPUT
        echo "EOF" >> $GITHUB_OUTPUT
    
    - name: Create Pull Request
      id: create-pr
      uses: peter-evans/create-pull-request@v5
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        branch: 'release/develop-to-main'
        base: 'main'
        title: 'Merge develop into main'
        body: |
          Автоматическое создание PR для слияния ветки develop в main
          
          Создано через GitHub Actions
          
          ## Коммиты для слияния:
          
          ${{ steps.get-commits.outputs.COMMITS }}
        draft: false
        delete-branch: false
    
    - name: Get PR URL
      id: get-pr-url
      run: |
        PR_URL=$(echo "${{ steps.create-pr.outputs.pull-request-number }}" | jq -r '.html_url')
        echo "PR_URL=$PR_URL" >> $GITHUB_OUTPUT
    
    - name: Notify Telegram
      uses: appleboy/telegram-action@master
      with:
        to: ${{ secrets.TG_CHAT }}
        token: ${{ secrets.TG_KEY }}
        message: |
          🔄 Запущен процесс создания нового релиза
          
          Создан Pull Request из ветки develop в main
          
          Ссылка на PR: ${{ steps.get-pr-url.outputs.PR_URL }}
          
          Ожидайте завершения проверки PR...

    - name: Notify Error to Telegram
      if: failure()
      uses: appleboy/telegram-action@master
      with:
        to: ${{ secrets.TG_CHAT }}
        token: ${{ secrets.TG_KEY }}
        message: |
          ❌ Ошибка при создании релиза
          
          Не удалось создать Pull Request из develop в main
          
          Ссылка на коммит: ${{ github.server_url }}/${{ github.repository }}/commit/${{ github.sha }}
          Ссылка на зафейленный экшн: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          
          Пожалуйста, проверьте логи и исправьте ошибку. 